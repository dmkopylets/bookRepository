{% extends 'base.html.twig' %}

{% block title %}Tag{% endblock %}

{% block body %}
    <h1>Tag</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tag.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ tag.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tag_index') }}" class="btn btn-secondary btn-sm">back to list</a>

    <a href="{{ path('app_tag_edit', {'id': tag.id}) }}" class="btn btn-secondary btn-sm">edit</a>

    {{ include('tag/_delete_form.html.twig') }}
{% endblock %}
